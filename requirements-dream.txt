# Dreamberry M2 dream engine (SDXL + ControlNet + IP-Adapter) + M4 SUPIR.
# Heavy GPU/ML stack; kept separate from the light M0/M1 dataset deps.
# Install: .venv/bin/pip install -r requirements-dream.txt

torch>=2.3
torchvision>=0.18
diffusers>=0.30
transformers>=4.44
accelerate>=0.33
safetensors>=0.4
huggingface_hub>=0.24
controlnet-aux>=0.0.9
opencv-python-headless>=4.9
python-dotenv>=1.0
numpy>=1.26
lpips>=0.1.4
# SUPIR (Fanghua-Yu) — also installed on Modal; local only if you run SUPIR here.
einops>=0.7
omegaconf>=2.3
open-clip-torch>=2.17
pytorch-lightning>=2.1
kornia>=0.6.9
