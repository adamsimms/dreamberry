.DS_Store
.env
.env.*
!.env.example
secrets.local.*
__pycache__/
*.pyc
.venv/
venv/
node_modules/
data/raw/
data/processed/
# Generated dream stills, sidecars, and cached control maps (M2 artifacts, not committed).
data/dream/
# M4 live weather packets (generated by scripts/fetch_live_weather.py --write)
data/live/
# M3 gate references (DINOv2 index, calibration) + eval outputs — regenerable, not committed.
data/gates/
# Manifests, curated lists, and weather packets ARE committed (M0 deliverables).
*.safetensors
*.ckpt
*.pt
outputs/
.modal/

# Local playground (notes, images, experiments). Contents are personal and
# never committed; Cursor can still read them for context. Tracked stub only:
# sketchpad/README.md
sketchpad/**
!sketchpad/README.md

# Machine-specific vault path and multi-root workspace (home-directory paths).
.cursor/rules/vault-local.mdc
*.code-workspace
